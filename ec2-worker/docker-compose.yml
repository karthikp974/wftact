services:
  followup-watcher:
    image: node:22-alpine
    container_name: wftact-followup
    working_dir: /app
    volumes:
      - ..:/app:ro
    env_file:
      - .env
    command: node scripts/send-followup-emails.mjs --watch
    restart: unless-stopped
